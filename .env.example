# Public canonical URL used for sitemap and social sharing metadata.
NEXT_PUBLIC_SITE_URL=https://www.example-lab.org

# Server-only Cloudflare secret. Use the same value configured on the Vercel API.
# Never prefix this variable with NEXT_PUBLIC_ or expose it in browser code.
AIMS_API_KEY=replace-with-a-private-shared-secret
